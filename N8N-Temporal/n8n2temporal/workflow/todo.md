以下是还需要待思考/优化/实现的点:

[]1、需要确定各个节点，他们的inputData到底是怎么样的
    每个节点可以获取到的数据分为以下几种：
        1、开始节点传入 -> express._global_中；
        2、变量节点设置 -> express._var_中
        3、某个节点的执行结果 -> 存储在execNodeFinalResults中
        4、signalData的值为 未上一个节点的输出（可能为流失输出，也可能为非流失输出） 
        ~~5、如果当前节点的上游节点是流式输出，当前节点又是只获取1 / 2 / 3的值，那么就会被重复执行多次，这个暂时不考虑~~
    所以我们的策略应该是：
        1、每个节点（除了开始、结束、变量节点），都需要指定他的入参值来源（上述1-4中的某一个）
        2、在node节点定义的地方，使用 . 来进行分割

[]2、continue-as-new 这里逻辑有问题，要重新设计

[]3、退出条件这里要更多验证是否有问题

[]4、python code节点的测试